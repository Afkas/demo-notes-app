export async function handler(event) {
  return {
    statusCode: 200,
    headers: { "Content-Type": "text/plain" },
    body: `I hate Hello, World Applications! Your request was received at ${event.requestContext.time}. ${event.` ,
  };
}
